// import { shallow } from "@vue/test-utils"
// import SecondNavBar from "@/components/Utils/SecondNavBar"

describe("SecondNavBar.vue", () => {
    it("placeholder text", () => {
        // this one has the same problem as the login test
    })

    // it("should be a vue instance", () => {
    //     const wrapper = shallow(SecondNavBar)

    //     expect(wrapper.isVueInstance()).toBeTruthy()
    // })

    // TODO: test both ways of viewing (logged and guest)
})
